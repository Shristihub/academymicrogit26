spring:
#datasource properties
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/ekartdb
#jpa specific properties
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false 
message: Hello!!!
